RewriteEngine On
RewriteBase /

# Nếu file hoặc thư mục không tồn tại, chuyển về index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Chuyển mọi thứ về index.php và giữ nguyên đường dẫn
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
